apiVersion: rosshield.io/v1
kind: Check
metadata:
    id: 6.2.1.2
    title: 6.2.1.2 Ensure auditd service is enabled and active (Automated)
    description: Turn on the auditd daemon to record system events.
    severity: medium
spec:
    auditCommand: |-
        bash -c 'out="$(systemctl is-active auditd | grep '\''^active'\'' 2>/dev/null)"
        if [ -n "$out" ]; then
          printf '\''%s\n'\'' "** PASS **"
        else
          printf '\''%s\n'\'' "** FAIL **"
        fi
        '
    evaluationRule:
        op: contains
        value: '** PASS **'
    rationale: |-
        The capturing of system events provides system administrators with information to allow
        them to determine if unauthorized access to their system is occurring.
    fixGuidance: |-
        Run the following commands to unmask, enable and start auditd:
        # systemctl unmask auditd
        # systemctl enable auditd
        # systemctl start auditd
