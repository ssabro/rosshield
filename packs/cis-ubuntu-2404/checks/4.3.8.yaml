apiVersion: rosshield.io/v1
kind: Check
metadata:
    id: 4.3.8
    title: 4.3.8 Ensure nftables default deny firewall policy (Automated)
    description: |-
        Base chain policy is the default verdict that will be applied to packets reaching the end
        of the chain.
    severity: medium
spec:
    auditCommand: |-
        bash -c 'missing=0
        out_0=$(nft list ruleset | grep '\''hook input'\'' 2>/dev/null)
        printf '\''%s'\'' "$out_0" | grep -qF -- "type filter hook input priority 0; policy drop;" || { printf '\''miss-0: expected %s\n'\'' "type filter hook input priority 0; policy drop;"; missing=$((missing+1)); }
        out_1=$(nft list ruleset | grep '\''hook forward'\'' 2>/dev/null)
        printf '\''%s'\'' "$out_1" | grep -qF -- "type filter hook forward priority 0; policy drop;" || { printf '\''miss-1: expected %s\n'\'' "type filter hook forward priority 0; policy drop;"; missing=$((missing+1)); }
        out_2=$(nft list ruleset | grep '\''hook output'\'' 2>/dev/null)
        printf '\''%s'\'' "$out_2" | grep -qF -- "type filter hook output priority 0; policy drop;" || { printf '\''miss-2: expected %s\n'\'' "type filter hook output priority 0; policy drop;"; missing=$((missing+1)); }
        if [ "$missing" -eq 0 ]; then printf '\''** PASS **\n'\''; else printf '\''** FAIL **\n'\''; fi'
    evaluationRule:
        op: contains
        value: '** PASS **'
    rationale: |-
        There are two policies: accept (Default) and drop. If the policy is set to accept, the
        firewall will accept any packet that is not configured to be denied and the packet will
        continue transversing the network stack.
        It is easier to allow list acceptable usage than to deny list unacceptable usage.
        Note:
        • Allow port 22(ssh) needs to be updated to only allow systems requiring ssh
        connectivity to connect, as per site policy.
        • Changing firewall settings while connected over network can result in being
        locked out of the system.
        Impact:
        If configuring nftables over ssh, creating a base chain with a policy of drop will cause
        loss of connectivity.
        Ensure that a rule allowing ssh has been added to the base chain prior to setting the
        base chain's policy to drop
    fixGuidance: |-
        Run the following command for the base chains with the input, forward, and output
        hooks to implement a default DROP policy:
        # nft chain <table family> <table name> <chain name> { policy drop \; }
        Example:
        # nft chain inet filter input { policy drop \; }
        # nft chain inet filter forward { policy drop \; }
        # nft chain inet filter output { policy drop \; }
        Default Value:
        accept
