apiVersion: rosshield.io/v1
kind: SelfTest
metadata:
    checkId: 6.1.1.3
spec:
    cases:
        - name: "default review verdict (no audit cmd execution)"
          input:
              stdout: ""
              stderr: ""
              exitCode: 0
          expectedOutcome: INDETERMINATE
        - name: "audit input ignored — verdict still review"
          input:
              stdout: "SystemMaxUse=1G\nSystemKeepFree=500M\nMaxFileSec=1month"
              stderr: ""
              exitCode: 0
          expectedOutcome: INDETERMINATE
