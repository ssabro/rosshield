# Lodestar v0.6.3 — Release Notes

**Release date**: 2026-05-19
**기준 commit**: `1ac5f35` (main)
**이전 release**: [v0.6.2](https://github.com/ssabro/rosshield/releases/tag/v0.6.2) (2026-05-19, ROS2 Round 3 + hardcoded 영어)

---

## TL;DR

**Patch release — Phase 5~7 carryover 일소 round**. design doc 5건 마감 + 작은 본체 3건 일소 (E22-F BOOLEAN + CIS Manual 5건 + Optimistic+Undo). sub-agent 5+3 병렬 dispatch + 도메인 충돌 0. 회귀 0. Breaking 0.

| 항목 | 값 |
|---|---|
| v0.6.2 이후 commit | 9 |
| 파일 변경 | 90+ files / +2700 +1100 +570 +400 + dist |
| 설계 doc 신규 | 4 (LLM private · Multi-region HA · Audit rotation · CIS Manual fixture) |
| 본체 신규 | 3 (E22-F BOOLEAN + CIS Manual 5건 + Optimistic+Undo) |
| CIS Ubuntu 24.04 cover | 100% (false-FAIL 0) |
| Breaking change | **0건** |
| 신규 dep | 0 |

---

## Added

### 설계 doc 4건 (Phase 5~7 큰 carryover)

| doc | commit | 줄 수 | 옵션 | Stage | 결정 default |
|---|---|---|---|---|---|
| `llm-private-deployment-design.md` | `d5b075f` | 431 | 5 | 5 | 7 (옵션 C: Ollama edge + vLLM 데이터 센터) |
| `multi-region-ha-design.md` | `5afbed1` | 412 | 4 | 7 | 5 (옵션 A: PG logical+DNS) |
| `audit-chain-rotation-design.md` | `be9239c` | 457 | 5 | 6 | 10 (옵션 A: 월 1회 + S3) |
| `cis-manual-fixture-21-design.md` | `465258c` | 406 | 5 | 5 | 7 (옵션 A+B: env-var skip + manual prompt) |

E22-F BOOLEAN design doc (`c8eeffa`, 418줄)는 직전 round 이미 main 적용.

### CIS Ubuntu 24.04 100% cover 도달 (`f1b2dc5`)

잔여 placeholder 5건 → `op: manual` 정식 fixture로 회수:

| CIS ID | env var | 의미 |
|---|---|---|
| 1.2.1.2 | `ROSSHIELD_CIS_1_2_1_2_POLICY` | apt source 도메인 화이트리스트 |
| 6.1.2.1.2 | `ROSSHIELD_CIS_6_1_2_1_2_POLICY` | journal upload URL+cert |
| 6.1.3.5 | `ROSSHIELD_CIS_6_1_3_5_POLICY` | audit rule 정규식 패턴 |
| 6.1.3.6 | `ROSSHIELD_CIS_6_1_3_6_POLICY` | audit remote loghost |
| 6.1.3.8 | `ROSSHIELD_CIS_6_1_3_8_POLICY` | audit log rotation 정책 |

- **manual yaml 17건 도달** (기존 12 + 신규 5)
- placeholder 0 — **false-FAIL emit 0**
- 신규 onboarding doc `docs/onboarding/cis-customer-policy.md` (~170줄)

### Optimistic update + Undo window (`6ec135f`)

D-UI-1 P0 carryover (Frontend P0 #5 + A11y P0 #3):

#### Optimistic update — 5 mutation
| hook | query cache 즉시 변경 |
|---|---|
| `useDismissInsight` | `['insights']` row 제거 |
| `useCancelScan` | `['scans', id]` + list status='cancelled' |
| `useDeleteRobot` | `['robots']` row 제거 |
| `useDeleteFleet` | `['fleets']` row 제거 |
| `useDeleteWebhook` | `['webhooks']` row 제거 |

표준 패턴: `cancelQueries` → snapshot → `setQueriesData` → `onError` rollback → `onSettled` invalidate.

#### Undo window — 5 destructive handler
신규 `web/src/lib/undoable.ts` — ConfirmDialog 후 5초 sonner toast + undo button. undo click 시 mutation 호출 자체 보류 (서버 round-trip 0 + audit chain noise 0).

5 페이지 (findings · scans · robots · fleets · integrations) destructive handler 일괄 적용.

i18n 신규 키 3건 (`common.undo` · `common.cancelled` · `common.action.scheduled`).

---

## Fixed

### E22-F R30-1.2 BOOLEAN 회수 (`aa78984`)

PG SMALLINT → BOOLEAN 5 컬럼 (마이그레이션 `0031_boolean_native_recovery`):

| # | 테이블·컬럼 | DEFAULT |
|---|---|---|
| 1 | `roles.is_system` | FALSE |
| 2 | `compliance_profiles.enabled` | TRUE |
| 3 | `webhook_endpoints.enabled` | TRUE |
| 4 | `webhook_deliveries.succeeded` | FALSE |
| 5 | `sso_providers.enabled` | TRUE |

#### Application code 변경 (4 sqliterepo + 1 test)
- `tenant/sqliterepo/repo.go`: `insertRole` BIND · `scanRoleRow` SCAN · `assembleRole` 시그니처
- `compliance/sqliterepo/repo.go`: `CreateProfile` INSERT · `scanProfileRow`
- `integration/webhook/sqliterepo/repo.go`: 4 site BIND/SCAN + WHERE/UPDATE parameterized + `boolToInt` 헬퍼 제거
- `tenant/sso/sqliterepo/repo.go`: `CreateProvider`/`UpdateProvider` INSERT/UPDATE + scan
- `migrations_test.go`: `expectedSequences` 0031 추가

#### 신규 integration test
`pgnative_boolean_integration_test.go` (5 test, 300 lines, testcontainers):
- 5 컬럼 `information_schema.columns.data_type = 'boolean'` 검증
- 컬럼별 round-trip 4 test
- WHERE/UPDATE parameterized bool 검증

#### sqliterepo 회귀 0
modernc.org/sqlite의 bool BIND/SCAN 호환성 실증 — design doc Stage 1 진단 결과 OK.

---

## Breaking Changes

**0건**. 라우팅 · API · 도메인 변경 0. 기존 hook 사용 변경 0. sqliterepo 외부 API 변경 0.

---

## Migration Notes

### DB 마이그레이션 (자동)
PG 사용 customer는 server 첫 부팅 시 `0031_boolean_native_recovery.up.sql` 자동 적용 (`ALTER COLUMN ... TYPE BOOLEAN USING (col = 1)`). 데이터 손실 0. 롤백은 `down.sql` (BOOLEAN → SMALLINT). sqlite customer는 영향 0 (driver의 INTEGER ↔ bool 자동 변환).

### 빌드
```bash
git fetch --tags && git checkout v0.6.3
cd web && pnpm install --frozen-lockfile && pnpm build
cd .. && go build -o bin/rosshield-server ./cmd/rosshield-server
./bin/rosshield-server --addr 127.0.0.1:8080 --data-dir ./run-data
```

### CIS Ubuntu 24.04 customer onboarding
신규 잔여 5건 env var 정의 — `docs/onboarding/cis-customer-policy.md` 참조.

---

## 한계 (carryover)

### Phase 5~7 큰 본체 (design doc → 코드 본체)
- **LLM private deployment** 본체 (vLLM driver + Ollama 강화) — ~730줄 / 3일
- **Audit chain rotation** 본체 (월 1회 checkpoint + S3) — ~2000줄 / 9~10일
- **Multi-region HA** 본체 (PG logical+DNS) — ~1550줄 / 3~4주

### Stage 5 polish
- **D-UI-1 Stage 5** — axe-core a11y 측정 + visual rhythm
- **integrations delivery detail dialog**

---

## Verification

```bash
git fetch --tags && git checkout v0.6.3
cd web && pnpm install --frozen-lockfile && pnpm build
cd .. && go vet ./...                                    # PASS
go test -count=1 ./internal/domain/... ./internal/builtin/...  # 26 패키지 PASS
go test -tags=integration ./internal/platform/storage/postgres/...  # boolean+migrations PASS
go build -o bin/rosshield-server ./cmd/rosshield-server
./bin/rosshield-server --addr 127.0.0.1:8080 --data-dir ./run-data
```

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md)
- [v0.6.2 release notes](v0.6.2.md) — base
- 4 design doc (Phase 5~7 carryover)
