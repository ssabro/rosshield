# Lodestar v0.7.4 — Release Notes

**Release date**: 2026-05-20
**기준 commit**: `24fb4e6` (main)
**이전 release**: [v0.7.3](v0.7.3.md) (2026-05-20, Stage 5b drill-down a11y)

---

## TL;DR

**Phase 8 Multi-region cutover 운영 단위 완성 patch** — design + ops + Terraform IaC + failover runbook 4 종을 한 release에 묶어 customer가 cross-region failover 환경을 자체 구축·운영 가능한 완전 set 마련.

| 항목 | 값 |
|---|---|
| v0.7.3 이후 commit | 4 |
| 신규 design doc | 1 (`multi-region-ha-stage4-design.md`) |
| 신규 ops doc | 2 (`multi-region-dns.md`, `multi-region-failover-runbook.md`) |
| 신규 Terraform module | 1 (`deploy/terraform/multi-region-ha/`, 9 파일 411줄) |
| 코드 변경 | 0건 (docs + IaC만) |
| Breaking change | **0건** |

---

## Added

### Phase 8 Stage 4 — DNS routing + failover records 설계 (`1f1d192`)

`docs/design/notes/multi-region-ha-stage4-design.md` (12 섹션, 360+ 줄).

#### 핵심 설계
- Route53 failover record + ALB target health + manual application promote
- RTO ≤ 5분 분해 (DNS layer ≤ 60초 + 운영자 promote ≤ 2분 + restart ≤ 60초)
- 4 DNS provider 비교 (Route53 / Cloudflare / NS1 / 자체 BIND·PowerDNS)
- on-prem customer 자체 DNS 가이드 (air-gap · 사설망 · 정부 customer)

#### D-MR-4 sub-decisions 5종 (권장 default 수용)

| ID | 결정 |
|---|---|
| D-MR-4-A Primary DNS | Route53 + Cloudflare/자체 DNS alternative |
| D-MR-4-B TTL | 60s (RTO ≤ 60초 보장) |
| D-MR-4-C Health check | 3회 연속 fail / 30s interval |
| D-MR-4-D 자동 vs manual | DNS 자동 + application manual (split-brain 회피) |
| D-MR-4-E IaC | Terraform |

### Phase 8 Stage 4.2 — DNS routing ops 가이드 (`c0b1222`)

`docs/operations/multi-region-dns.md` (9 섹션, 340+ 줄).

- Route53 Health check + Failover record + ALB target health setup
- 운영자 cutover 절차 — 자동 90초 + manual promote 2분 = RTO 약 4분
- Primary 복구 후 replication 방향 reverse + Tokyo 유지 vs Seoul 복귀 선택
- Cloudflare alternative (Load Balancer + Pool + Monitor, $5/month Pro + Load Balancer)
- 자체 DNS — BIND zone file + PowerDNS API script
- Prometheus 메트릭 + Alertmanager rule 예시 (replication lag · failover counter)

### Phase 8 Stage 4.3 — Terraform IaC sample (`9a59732`)

`deploy/terraform/multi-region-ha/` (9 파일, 411줄):

```
deploy/terraform/multi-region-ha/
├── README.md                                     # customer 사용법
├── main.tf                                       # root config (us-east-1 provider)
├── variables.tf                                  # root input + validation
├── outputs.tf                                    # record FQDN + health check ID
├── .gitignore                                    # envs/*.tfvars 보호
├── modules/
│   └── route53-failover/                         # 재사용 module
│       ├── main.tf                               # health check + failover record
│       ├── variables.tf
│       └── outputs.tf
└── envs/
    └── example.tfvars                            # customer 복제용 예시
```

customer 사용:
```bash
cp envs/example.tfvars envs/prod.tfvars
# prod.tfvars 편집 (domain·hosted_zone·endpoint)
terraform init
terraform plan -var-file=envs/prod.tfvars
terraform apply -var-file=envs/prod.tfvars
```

ALB·EC2·PG 본체는 customer infra 가정 — endpoint FQDN만 입력 받는 form.

### Phase 8 Stage 5 — Failover runbook (`24fb4e6`)

`docs/operations/multi-region-failover-runbook.md` (11 섹션, 350+ 줄).

운영자 incident 대응 step-by-step. RTO ≤ 5분 목표.

- §0 핵심 원칙: DNS 자동 + application manual / time-boxed escalation
- §1 사전 준비: SSH 키 + PagerDuty + 정보 vault + 월 1회 staging drill
- §2 감지 (T+0~2분): 알림 → Slack 통지 → DNS cutover 확인 → false positive 분기
- §3 promote (T+2~4분): SSH → `pg_promote()` → rosshield-server restart → leader-election 검증
- §4 검증 + §5 customer 통지
- §6 Escalation: false positive · pg_promote 실패 · restart 실패 → secondary on-call
- §7 Roll-back: 잘못 promote 시 Tokyo 격리 + DNS cutback + PG base backup 복원
- §8 Primary 복구: 옵션 A Tokyo 유지 (권장) vs 옵션 B Seoul 복귀
- §9 사후 분석: RTO·RPO 실측 + postmortem template + runbook 갱신 PR
- §10 Quick reference card (printable timeline)

---

## Breaking Changes

**0건**. 모든 변경은 docs/IaC. application 코드 영향 없음.

---

## Phase 8 Multi-region HA 누적 진척

| Stage | 상태 | release |
|---|---|---|
| 1·2 replication metadata + standby middleware | ✅ | v0.6.6 |
| 3 PUBLICATION/SUBSCRIPTION 자동 setup | ✅ | v0.6.8 |
| 3 후속 publication sync + slot cleanup | ✅ | v0.7.0 |
| **4.1 design draft** | ✅ | **v0.7.4** |
| **4.2 ops doc** | ✅ | **v0.7.4** |
| **4.3 Terraform IaC** | ✅ | **v0.7.4** |
| **5 failover runbook** | ✅ | **v0.7.4** |
| 4.4 Cloudflare alternative Terraform module | ⏳ | 후속 |
| 4.5 BIND/PowerDNS Terraform sample 분리 | ⏳ (ops doc cover) | 후속 |
| 4.6 staging 검증 | ⏳ 외부 customer | 후속 |
| 6 자동 failover research (Patroni/Stolon) | ⏳ Phase 9+ | 후속 |
| 7 testcontainers e2e | ⏳ MR.T1~T8 | 후속 |

**v0.7.4로 Phase 8 cross-region cutover 운영 단위 완성** — customer는 design 정독 + ops 따라하기 + IaC apply + runbook으로 incident 대응까지 한 release로 마련.

---

## Migration Notes

### customer 적용 절차

1. `multi-region-ha-stage4-design.md` 정독 (옵션 비교 + 결정 근거)
2. `multi-region-dns.md`로 Route53 setup (또는 Cloudflare / 자체 DNS)
3. `deploy/terraform/multi-region-ha/`로 IaC plan/apply
4. `multi-region-failover-runbook.md` 인쇄 + 운영자 교육
5. staging 환경에서 월 1회 drill 실시

### 빌드

```bash
git fetch --tags && git checkout v0.7.4
cd web && pnpm install --frozen-lockfile && pnpm build
cd .. && go build -o bin/rosshield-server ./cmd/rosshield-server
```

code 변경 0건이라 v0.7.3 binary와 동일.

---

## 한계 (carryover)

- **4.4 Cloudflare Terraform module** — ops doc §5에 manual setup 가이드는 있지만 Terraform module은 후속
- **4.5 BIND/PowerDNS Terraform sample** — ops doc §6에 기본 가이드 cover, Terraform sample은 별 module
- **4.6 staging 검증** — Lodestar 본 repo 책임 외, customer 측 staging env 의존
- **Stage 6 자동 failover** — Patroni/Stolon research, Phase 9+ deferral
- **Stage 7 testcontainers e2e** — MR.T1~T8 통합 test, 후속 round
- **C5b-6/C5b-7/C5b-8/C5b-9** — a11y 잔여 carryover
- **R-D8 청구권 명세서** — 사용자 외부 트랙
- **E36 레퍼런스 HW burn-in** — 사용자 hands-on

---

## Verification

```bash
git fetch --tags && git checkout v0.7.4

# docs 검증
ls -lh docs/design/notes/multi-region-ha-stage4-design.md
ls -lh docs/operations/multi-region-dns.md
ls -lh docs/operations/multi-region-failover-runbook.md

# Terraform IaC syntax 검증
cd deploy/terraform/multi-region-ha
terraform fmt -check -recursive
terraform validate

# 코드 회귀 0 (변경 없음)
cd ../../..
go test -count=1 ./...
cd web && pnpm exec vitest run
```

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md)
- [v0.7.3 release notes](v0.7.3.md) — direct base
- [multi-region-ha-design.md](../design/notes/multi-region-ha-design.md) — Phase 8 전체 epic (Stage 1~7)
- [multi-region-ha-setup.md](../onboarding/multi-region-ha-setup.md) — PG replication 전제 조건 setup
