# Lodestar v0.6.5 — Release Notes

**Release date**: 2026-05-20
**기준 commit**: `de9e380` (main)
**이전 release**: [v0.6.4](https://github.com/ssabro/rosshield/releases/tag/v0.6.4) (2026-05-20, LLM private + Stage 5 polish)

---

## TL;DR

**Minor patch — Audit chain rotation 본체 + Stage 5b a11y/spacing**. design doc 4건 중 Audit rotation 본체 Stage 1~3 진입 (가장 큰 carryover). color-contrast 실측 e2e 자동화 + drill-down spacing 일관화. 회귀 0. Breaking 0.

| 항목 | 값 |
|---|---|
| v0.6.4 이후 commit | 3 |
| 파일 변경 | 21 files / +2071 / -5 |
| 신규 마이그레이션 | 0032 (audit_rotation_segments + 불변성 트리거) |
| 신규 패키지 | `internal/domain/audit/rotation/` (8 파일, ~1000줄) |
| 신규 e2e | Playwright color-contrast 10 test |
| 신규 dep | @axe-core/playwright (devDep) |
| Breaking change | **0건** |

---

## Added

### Audit chain rotation Stage 1~3 (`de9e380`)

design doc `audit-chain-rotation-design.md` 권장 옵션 A (월 1회 + S3) 진입. 10 결정 default (D-AR-1 ~ D-AR-10) 모두 권장값.

#### 마이그레이션 0032
- **PG**: `audit_rotation_segments` 테이블 — tenant_id · segment_number · started_at/ended_at · first_entry_id/last_entry_id · entry_count · segment_hash · archive_uri · archive_sha256 · cosign_bundle · created_at
- UNIQUE (tenant_id, segment_number) + CHECK (entry_count > 0)
- **P9 불변성 트리거** (PG: PL/pgSQL · SQLite: RAISE(ABORT)) — UPDATE/DELETE 차단
- sqlite 동등 마이그레이션
- migration test `expectedSequences` + `wantVersion` 갱신

#### `internal/domain/audit/rotation/` 신규 패키지 (8 파일)

| 파일 | 역할 | 줄 |
|---|---|---|
| `policy.go` | RotationPolicy + env override (FREQUENCY/HOT_RETENTION_DAYS/COLD_BACKEND) + Validate | 142 |
| `backend.go` | Backend interface (Put/Get/Exists/Scheme) | 33 |
| `backend_file.go` | FileBackend (Apache-2.0) — file:// + atomic write+rename + path escape 거부 | 158 |
| `backend_s3.go` | S3Backend stub (`!enterprise` build tag) | 58 |
| `builder.go` | BuildSegment + ComputeSegmentHash (sequential sha256 fold) | 169 |
| `archiver.go` | Archive (tar.gz: manifest.json + entries.ndjson) | 141 |
| `rotation.go` | Rotator.Rotate (단일 Tx: build + archive + insert + emit) | 280 |
| `rotation_test.go` | 단위 test | 581 |

#### `audit.rotate.complete` entry — chain의 정상 link
rotation 완료 시 audit chain에 link 추가 (외부 검증 누락 0):
- action: `audit.rotate.complete`
- target: `(audit_rotation_segment, <segmentNumber>)`
- payload: canonical JSON (segmentNumber, segmentHash, archiveUri, archiveSha256, ...)

#### 환경 변수 신규
- `ROSSHIELD_AUDIT_ROTATION_FREQUENCY` (keyword: `monthly`, `weekly` 또는 Go duration)
- `ROSSHIELD_AUDIT_HOT_RETENTION_DAYS` (양수)
- `ROSSHIELD_AUDIT_COLD_BACKEND` (`file`, `s3`)

### Stage 5b color-contrast 실측 e2e (`a1f7353`)

Playwright + @axe-core/playwright — jsdom 한계 회피.

#### 신규
- `web/playwright/tests/color-contrast.spec.ts` (99줄) — 5 페이지 × light/dark = **10 test**
- `web/playwright/helpers.ts` `applyThemeMode` helper
- `web/playwright/README.md` color-contrast 절
- `web/package.json` devDep: `@axe-core/playwright@^4.11.3`

#### 핵심 설계
- rule scope: `withRules(['color-contrast'])` 단독 — 구조적 a11y는 vitest-axe cover
- light/dark 토글: spec 내부 helper (project 분기 시 globalSetup 2번 risk 회피)
- page ready marker: `expect(getByText).toBeVisible()` — skeleton false PASS 방지
- 실 실행 carryover (C5b-1): server 띄우기 + 결과 매트릭스

### Stage 5b drill-down spacing 통일 (`ec28349`)

직전 Stage 5 일반 페이지 일관화 완료 후, drill-down + 잔여 일반 페이지 cover.

#### 일관화 적용 (2 페이지)
- `compliance.tsx` root `space-y-6` → `space-y-4`
- `fleets.$fleetId.tsx` 3 분기 (loading/error/main) `space-y-6` → `space-y-4`

#### 미적용 (1 페이지 carryover)
- `robots.$robotId.tsx` — 5+ 카드 + 위험 액션 분리 (delete card 강조) → `space-y-6` 유지

**최종 분포**: 19 페이지 중 `space-y-4` × **18**, `space-y-6` × 1.

---

## Breaking Changes

**0건**. audit chain 기존 entry 형식 변경 0. 라우팅 · API · 도메인 변경 0.

---

## Migration Notes

### DB 마이그레이션 (자동)
PG/sqlite customer 모두 server 첫 부팅 시 `0032_audit_rotation` 자동 적용. 데이터 손실 0.

### Audit rotation 활성화 (옵션 — default 비활성)
본 round는 manual API + scaffold만. cron 자동 rotation은 별 epic (Stage 4~6 carryover).

### Stage 5b color-contrast 실 실행
```bash
cd web
pnpm exec playwright install --with-deps chromium  # 첫 1회
# server 띄운 후:
pnpm exec playwright test web/playwright/tests/color-contrast.spec.ts
```

### 빌드
```bash
git fetch --tags && git checkout v0.6.5
cd web && pnpm install --frozen-lockfile && pnpm build
cd .. && go build -o bin/rosshield-server ./cmd/rosshield-server
./bin/rosshield-server --addr 127.0.0.1:8080 --data-dir ./run-data
```

---

## 한계 (carryover)

### Audit rotation Stage 4~6 carryover
- **hot GC** (rotated row DELETE) — audit 트리거 우회 필요, 별 epic
- **cosign keyless Sigstore bundle** — column 예약, 실서명 별 epic
- **S3 실 SDK 호출** — enterprise build tag + AWS SDK v2
- **verify CLI 확장** — 별 epic
- **cross-witness rotation segment fold-in** — 별 epic
- **cron 자동 rotation job** — manual API only
- **prev_segment_hash chain** (segment 간 link) — Stage 5 schema 확장

### Stage 5b color-contrast carryover
- **C5b-1** 10 케이스 실 실행 + 결과 매트릭스
- **C5b-2** CI workflow 임계치 정책
- **C5b-3** drill-down 페이지 추가 (`fleets.$id`, `robots.$id`, `packs.$pack.checks.$check`)
- **C5b-4** Login/Invitation accept 페이지 (인증 전 helper)
- **C5b-5** Settings/Users/System 페이지 (admin role + form-heavy)

### 기타
- **Multi-region HA 본체** (3~4주, 별 round)
- **3rd party (Radix/cmdk/sonner) a11y 매트릭스**
- **Bundle size 800KB warning** — manualChunks 최적화

---

## Verification

```bash
git fetch --tags && git checkout v0.6.5
cd web && pnpm install --frozen-lockfile && pnpm build
cd .. && go vet ./...                                  # PASS
go test -count=1 ./internal/domain/audit/...           # rotation + sqliterepo + checkpoint PASS
go test -tags=integration ./internal/platform/storage/postgres/...
go build -o bin/rosshield-server ./cmd/rosshield-server
./bin/rosshield-server --addr 127.0.0.1:8080 --data-dir ./run-data
```

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md)
- [v0.6.4 release notes](v0.6.4.md) — base
- [docs/design/notes/audit-chain-rotation-design.md](../design/notes/audit-chain-rotation-design.md) — 본체 spec
- [docs/design/notes/ui-stage5b-color-contrast-report.md](../design/notes/ui-stage5b-color-contrast-report.md)
- [docs/design/notes/ui-stage5b-spacing-report.md](../design/notes/ui-stage5b-spacing-report.md)
