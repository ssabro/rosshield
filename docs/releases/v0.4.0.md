# Lodestar v0.4.0 — Release Notes

**Release date**: 2026-05-18
**Codename**: rosshield (코드 네임스페이스) / **Lodestar** (제품 브랜드, D-P7-1 확정)
**기준 commit**: `01e41fc` (main)
**이전 release**: [v0.3.0](https://github.com/ssabro/rosshield/releases/tag/v0.3.0) (2026-05-18, Phase 5 + Phase 6 후보 1 마감)

---

## TL;DR

본 release는 **Phase 7 코드 트랙(R-BRAND + R-LICENSE + R-D8 4/4) 완전 마감 + ROS2 baseline pack Round 1 MVP 6/8 카테고리 cover**를 처리한 라운드입니다. 출원 완료 가정 trigger(D-P7-5, 사용자 결정 2026-05-18)로 unblock된 4 epic 중 코드 트랙 3종(R-BRAND·R-LICENSE·R-D8)을 모두 마감 + 1순위 결합 청구항 4 본체를 enterprise build tag 격리로 보호. 회귀 0건.

| 항목 | 값 |
|---|---|
| v0.3.0 이후 commit | 20 (`v0.3.0..01e41fc`) |
| 파일 변경 | 81 files / +6555 / -39 |
| Phase 7 epic 마감 | 3/4 (R-BRAND ✅ Stage 1 · R-LICENSE ✅ · R-D8 ✅ 4/4) |
| R-D8 청구권 본체 | 4/4 (A-1 cross-witness · B-1 multihash · C-1 wasmrt · D-3 robotid) |
| ROS2 pack 카테고리 cover | 6/8 (C1·C2·C3·C6·C7·C8) |
| 신규 마이그레이션 | 0건 (코드 영역 변경) |
| Breaking change | **0건** |
| 신규 dep | 2건 (`lukechampine.com/blake3 v1.4.1` + `tetratelabs/wazero v1.11.0`, 모두 enterprise build tag 한정) |
| enterprise 단위 PASS | 129+ (crosswitness 17 + multihash 48 + wasmrt 45 + robotid 19+) |
| sub-agent worktree 병렬 dispatch | 31회 누적 |

---

## Highlights

### Phase 7 — R-BRAND (Lodestar 브랜드 확정)

D-P7-1 권장 default 'Lodestar' 채택 — Top 3 후보(Custos·Lodestar·Praxis) 중 등록 가능성 최우선.

- `feat(brand)` R-BRAND Stage 1 — `<ProductName>` placeholder → 'Lodestar' 사용자 대면 6 파일 교체 (`3e3d892`)
- `feat(brand)` R-BRAND Stage 1 보완 — design/onboarding/web 잔여 9 파일 11 위치 교체 + d1-brand-candidates.md §5.6 확정 근거 (`20eddee`)
- **코드 네임스페이스 `rosshield` 보존** (Go 모듈 · CLI · YAML apiVersion · PWA manifest short_name 변경 0)

### Phase 7 — R-LICENSE (Open-core 라이선스 양분)

D-P7-2 권장 default 'BSL 1.1' 채택 — Change Date 2030-05-18(출원일 가정 + 4년) + Change License Apache-2.0.

- `docs(license)` LICENSE-ENTERPRISE 신규 — BSL 1.1 parameters (Licensed Work=`internal/enterprise/*`, Additional Use Grant non-competing production OK) (`ea8d5d7`)
- `docs(license)` NOTICE 신규 — third-party OSS attribution ~20 dep
- 기존 LICENSE(Apache 2.0) 보존 — 코어/enterprise 라이선스 양분 결선

### Phase 7 — R-D8 (D8 청구권 코드 분리, enterprise build tag) — **4/4 본체 완전 마감**

D-P7-4 우선순위 default(A-1→B-1→C-1→D-3) 정확 진행. 1순위 결합 청구항 4 본체 모두 `internal/enterprise/*` build tag 격리.

#### A-1 cross-witness fold-in (`b4e77eb`, 2 파일 +556)

`internal/enterprise/crosswitness/` — multi-fold hash chain + RFC 8785 canonical JSON + tenantId 사전식 정렬 + signedAt UTC RFC3339Nano. spec-candidate-A-draft.md 실시례 4 [0019]~[0020-4] 정확 일치. **17 단위 PASS**.

#### B-1 multi-hash evidence (`5292585`, 6 파일 +1371)

`internal/enterprise/multihash/` — SHA256 + BLAKE3 cross-check + JSONPath/line sub-hash + VerifyMode enum(ModeCoreSHA256 / ModeEnterpriseFull). **48 단위 PASS**. 신규 dep `lukechampine.com/blake3 v1.4.1`.

#### C-1 WASM sandboxed evaluator (`012fe3f`, 9 파일 +1515)

`internal/enterprise/wasmrt/` — wazero runtime + WASI 격리(filesystem 화이트리스트 + sock/fs 차단) + CPU timeout(context.WithTimeout + WithCloseOnContextDone) + memory limit + PolicyVerifier interface(NopVerifier + errVerifier). 결정론 hand-crafted WASM 4종(WebAssembly 1.0 spec 정확 준수, signed LEB128 + data segment active prefix). **45 단위 PASS**. 신규 dep `tetratelabs/wazero v1.11.0`.

#### D-3 robotid fingerprint (`b8bbae7`, 10 파일 +1176)

`internal/enterprise/robotid/` — `sha256(EK_cert ‖ sorted_macs ‖ cpu_serial ‖ salt)` fingerprint + tenant salt cross-tenant 분리 + MAC sort invariant + CPUSerial trim+lowercase + tpmOpener/ekLoader hook test seam. Linux 한정 tpm.go + collector.go + Windows stub. **19+ 단위 PASS**. `go-tpm-tools v0.4.8` indirect 활용(go.mod 변경 0).

### ROS2 baseline pack Round 1 (솔루션 핵심 차별화 영역)

design doc 옵션 B Round 1 권장 default(C1·C2·C3·C6·C7·C8) **카테고리 cover 6/8 = 75%** 도달. 9 check 총 + 9 selftest fixture(mock, D-ROS2-9 정확 준수).

- `feat(packs)` Round 1 Stage 1 — `packs/ros2-jazzy/` 신규 + C1 SROS2 + C6 distro(LTS/EOL/CLI) (`8eb3d7d`)
- `feat(packs)` Round 1 Stage 2 — C3 ROS_DOMAIN_ID 격리 + C7 RMW_IMPLEMENTATION (`edfba4f`)
- `feat(packs)` Round 1 Stage 3 — C8 governance.xml ENCRYPT topics (`f34f8b9`)
- `feat(packs)` Round 1 Stage 4 — C2 cmd_vel publisher count + ACL (`c6ea725`)

ros2_jazzy_fixture_test.go 동적 ReadDir round-trip — 9 check + 9 selftest 모두 자동 cover.

### Phase 7 통합 design doc (출원 완료 가정 trigger)

- `design(phase7)` D1 brand + E37 public + D8 청구권 분리 통합 design doc 815줄 (`342eb0e`)
- `docs(design)` 13-patent-strategy §13.6 출원 완료 가정 trigger 섹션 + §13.10 D-P7-5 결정 로그 (`61c3e61`)

사용자 결정("출원이 완료 된것으로 생각하고 작업을 이어 갔으면 좋겠어") 정확 인용 + 4 epic unblock 매핑.

---

## Breaking Changes

**0건**. 모든 추가는 enterprise build tag(`rosshield_enterprise`) 한정 또는 신규 패키지·파일. 코어 → enterprise import 0 (boundary_test 회귀 0).

---

## Migration Notes

- **DB 마이그레이션 0건** — 코드 영역 변경만.
- **enterprise build tag 빌드**: `make build-enterprise` 또는 `go build -tags rosshield_enterprise ./...` — 본 release에서 처음으로 4 청구권 본체가 컴파일 가능합니다.
- **코어 빌드**: 변경 0. 기존 `make build` 그대로 동작.

---

## Verification

```bash
# 1. 태그·tarball 검증 (다음 release 단계에서 cosign keyless 서명 자동 trigger)
git fetch --tags && git checkout v0.4.0

# 2. 코어 빌드 + 회귀
make ci   # vet + test + build
go test ./... -count=1

# 3. enterprise build tag 본체 + 단위
make build-enterprise
make test-enterprise   # 또는: go test -tags rosshield_enterprise -count=1 ./internal/enterprise/...
# 기대: crosswitness 17 + multihash 48 + wasmrt 45 + robotid 19+ = 129+ PASS

# 4. ROS2 pack round-trip
go test -count=1 ./internal/domain/benchmark/...   # 9 check + 9 selftest fixture 자동 cover

# 5. boundary 가드 (코어 → enterprise import 0 검증)
go test -count=1 ./internal/enterprise -run TestCorePackagesDoNotImportEnterprise
```

---

## 한계 (carryover, 다음 release 후보)

- **Phase 7 R-PUBLIC** — GitHub repo public 전환 + first external contribution baseline. 사용자 GitHub Settings 권한 필요(외부 트랙).
- **R-D8 청구권 후속 v2** — A-1 scheduler/anchor 보강 · B-1 wildcard JSONPath · C-1 실 cosign 서명 검증 · D-3 TPM Quote PCR 결합.
- **ROS2 pack Round 2** — C4(binary 무결성: apt source + colcon 산출) + C5(launch 안전: parameter injection). 1~1.5주 추정. paying customer trigger 권장.
- **첫 paying customer onboarding** — Phase 6 후보 1 R1 intake API + R2 walkthrough + R3 SLA 모두 v0.3.0에 마감. customer 진입 시 추가 Manual fixture + scanrun extras + LLM private deployment + multi-region HA 등 carryover trigger.
- **E36 레퍼런스 HW burn-in** — 사용자 hands-on. NUC 13 Pro + OptiPlex 7010 + TPM 봉인 + Secure Boot 측정.

---

## Contributors

- ssabro (Claude Code sub-agent worktree 병렬 dispatch 31회 누적 + 메인 fallback)

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md) — Keep a Changelog 1.1.0 형식
- [SESSION_HANDOFF.md](../../SESSION_HANDOFF.md) — 본 release 라운드 직전 한 줄 누적
- [docs/design/notes/phase7-public-transition-design.md](../design/notes/phase7-public-transition-design.md) — D-P7-1~6 권장 default 6건
- [docs/design/13-patent-strategy.md](../design/13-patent-strategy.md) §13.5 — 1순위 결합 청구항 4 본체
- [docs/design/notes/ros2-baseline-pack-design.md](../design/notes/ros2-baseline-pack-design.md) — 8 카테고리 + Round 1/2 분해
