# Lodestar v0.5.2 — Release Notes

**Release date**: 2026-05-19
**기준 commit**: `e636c1a` (main)
**이전 release**: [v0.5.1](https://github.com/ssabro/rosshield/releases/tag/v0.5.1) (2026-05-19, R-D8 v3 2/2 + SSHPool flaky fix)

---

## TL;DR

**Patch release** — ROS2 baseline pack Round 2 마감(C4 binary 무결성 + C5 launch 안전) + simulator integration CI 검증 통과. **ROS2 8/8 카테고리 cover 완성**. 회귀 0.

| 항목 | 값 |
|---|---|
| v0.5.1 이후 commit | 5 (`v0.5.1..e636c1a`) |
| 파일 변경 | 18 files / +762 / -5 |
| ROS2 카테고리 cover | **8/8 완성** (C1·C2·C3·C4·C5·C6·C7·C8) |
| 신규 check | 6 (C4×3 + C5×3) |
| 신규 simulator test | 7 (R-D8 v3 D-3 full verification 실 CI 검증) |
| Breaking change | **0건** |
| 신규 dep | 0 |
| sub-agent 병렬 | 4 (simulator 1 + ROS2 Round 2 C4/C5 2 + handoff 1) |

---

## Added

### ROS2 baseline pack Round 2 (`e885df0` + `ab1775b`)

8 카테고리 cover 완성 — paying customer demo에 8/8 ROS2 보안 baseline 풀스코프.

#### C4 binary 무결성 (`e885df0`, 3 check)
- `binary.apt_source_official` — `/etc/apt/sources.list.d/ros2*.list` 공식 packages.ros.org URL 검증
- `binary.no_world_writable_libs` — `/opt/ros/jazzy/lib/` world-writable 0
- `binary.systemd_unit_perms` — `/etc/systemd/system/ros2-*.service` owner=root + perms ≤ 0644

#### C5 launch 안전 (`ab1775b`, 3 check)
- `launch.no_world_writable_yaml` — launch/param yaml world-writable 0
- `launch.parameter_no_secret_inline` — `password|secret|token|api_key` inline value 검출 (env/vault 참조 제외)
- `launch.no_shell_exec` — `ExecuteProcess.*shell=True` grep

pack.yaml description: `"ROS2 Jazzy LTS Baseline Security Pack (Round 1+2 — C1·C2·C3·C4·C5·C6·C7·C8 8/8 cover 완성)"`

### TPM simulator integration test (`7f1e7f4`)

`internal/enterprise/robotid/quote_simulator_test.go` (266줄, 7 신규 test):
- round_trip + nonce_mismatch + PCR_tamper(PCR_Extend swap-in attack 모의) + signature_tamper + 결정론 + PCRDigest 결정론 + sentinel
- `tpm_integration` build tag (CI ubuntu-latest + libssl-dev + cgo)
- ci.yml tpm-integration job 갱신 — `-tags="tpm_integration rosshield_enterprise"` + robotid 패키지 추가
- **CI 7/7 ALL PASS 검증** (run 26070828560)
- R-D8 v3 D-3 full verification = mock 9 + simulator 7 + unit 16 = **32 cover**

---

## Breaking Changes

**0건**.

---

## Migration Notes

- **DB 마이그레이션 0건**.
- **신규 dep 0**.
- **ROS2 baseline pack 사용 예** (8/8 cover):
  ```bash
  rosshield scan --pack ros2-jazzy <robot>   # 15 check 실행 (8 카테고리)
  ```

---

## 한계 (carryover)

ROS2 Round 3 후보 6건 (paying customer 이상 trigger 권장):
- C4 — `apt_key_valid` (GPG 만료 검증) · `colcon_install_hash` (customer-supplied baseline digest) · `signed_packages_only` (apt-cache policy 파싱)
- C5 — `param_files_owner` (site path + ros-user 의존) · `argv_no_remote_url` (runtime arg) · `lifecycle_node_used` (safety-critical 분류)

v0.5.1과 동일:
- R-D8 v3 후속 추가 round
- E36 레퍼런스 HW burn-in (사용자 hands-on)
- R-D8 청구권 명세서 작성 (KIPO 출원용, 사용자 외부 트랙)

---

## Verification

```bash
git fetch --tags && git checkout v0.5.2
make ci                                       # 7/7 ALL PASS
make test-enterprise                          # 264+ 단위 PASS
go test -count=1 ./internal/domain/benchmark/...   # ROS2 pack round-trip 8 check + 8 selftest PASS
```

---

## Contributors

- ssabro (sub-agent worktree 병렬 dispatch 42회 누적)

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md)
- [v0.5.1 release notes](v0.5.1.md) — base
- [docs/design/notes/ros2-baseline-pack-design.md](../design/notes/ros2-baseline-pack-design.md) — 8 카테고리 spec
