# Lodestar v0.4.3 — Release Notes

**Release date**: 2026-05-19
**기준 commit**: `7fbde90` (main)
**이전 release**: [v0.4.2](https://github.com/ssabro/releases/tag/v0.4.2) (2026-05-19, snap configure hook plugs fix)

---

## TL;DR

**Patch release** — v0.4.2 Snap Build 또 fail (`internal/builtin/packs/embed.go //go:embed _archives/*.tar.gz` 부재). snap override-build script에 pack-tools build + keygen + make pack-archive step 추가. 자체 코드 회귀 0.

| 항목 | 값 |
|---|---|
| v0.4.2 이후 commit | 1 (`v0.4.2..7fbde90`) |
| 파일 변경 | 1 file / +11 / -1 |
| 자체 코드 회귀 | **0건** |
| Snap binary | v0.4.2 fail → v0.4.3에서 정상 빌드 예상 |

---

## Fixed

- `fix(snap)` override-build에 pack-archive step 추가 (`7fbde90`) — embed.go `//go:embed _archives/*.tar.gz` 패턴이 fresh clone에서 'no matching files found'로 fail. ci.yml/release-pipeline.yml과 동일 패턴 적용:
  1. `go build -o bin/pack-tools ./cmd/pack-tools`
  2. `bin/pack-tools keygen` (fallback dev signer)
  3. `make pack-archive`
  4. Go 바이너리 3종 빌드 (server·CLI·audit-verify) — 기존

---

## Phase 7 진척 (v0.4.0~v0.4.2와 동일)

R-BRAND ✅ · R-LICENSE ✅ · **R-D8 ✅ 4/4** · R-PUBLIC ⏸

---

## Breaking Changes

**0건**.

---

## 참조

- [CHANGELOG.md](../../CHANGELOG.md)
- [v0.4.2 release notes](v0.4.2.md) — base
- [v0.4.1 release notes](v0.4.1.md) — CI fix cascade 14 round
